# Makefile for Anki Browser Windsurf
# Provides simple commands for development, building, and previewing the app

.PHONY: dev build preview

# Install dependencies and start the development server
dev:
	npm install
	npm run dev

# Install exact dependencies and build for production
build:
	npm ci
	npm run build

# Preview the production build (serves from dist)
preview:
	npm run preview
